services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: lumina-vector-db
    ports:
      - "6333:6333"   # HTTP API
      - "6334:6334"   # gRPC API
    volumes:
      - ./qdrant_data:/qdrant/storage:z
    restart: always